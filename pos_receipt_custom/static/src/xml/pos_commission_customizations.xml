<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="point_of_sale.ControlButtons" t-inherit="point_of_sale.ControlButtons" t-inherit-mode="extension">
        <xpath expr="//SelectPartnerButton[@partner='partner']" position="after">
            <div class="control-buttons" t-if="!currentOrder?.isEmpty() and currentOrder.config.module_pos_discount" >
                <button class="btn btn-secondary btn-lg lh-lg text-truncate w-auto discount-button" t-on-click="showDiscountPopup">
                    <i class="fa fa-percent"/> Discount
                </button>
            </div>
            <!-- <ActionpadWidget
                partner="currentOrder?.getPartner()" 
                onClickMore.bind="displayAllControlPopup"
                actionName.translate="Set Commission Members"
                actionToTrigger="() => this.showCommissionView()"
                showActionButton="!currentOrder?.isEmpty()"
                fastValidate="(paymentMethod) => this.fastValidate(paymentMethod)"
            /> -->
        </xpath>
    </t>
</templates>